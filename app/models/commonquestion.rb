class CommonQuestion
  def Initialize(question,answer,slug)
    @question = question
    @answer = answer
    @slug = slug
    if @slug == "How much does it cost"
      return "How-much-does-it-cost"
    else
    end
  end
end
