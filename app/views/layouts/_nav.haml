%nav.navbar.navbar-inverse.navbar-fixed-top
  .container
    .row
      .navbar-header.col-md-12
        .pull-right
          -if current_user
            = link_to current_user.full_name, user_path(current_user.id), class: "navbar-brand"
            = link_to 'Sign Out', sign_out_path, class:"navbar-brand"
          -else
            = link_to 'Sign Up', sign_up_path, class: "navbar-brand"
            = link_to 'Sign In', sign_in_path, class: "navbar-brand"
        = link_to "gCamp", root_path, class:"navbar-brand"
