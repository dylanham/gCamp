.page-header
  .row
    .col-md-12
      .pull-right
        %h1= link_to 'Edit',edit_task_path(@task), class:'btn btn-info'
      - if @task.complete
        %h1
          %s=@task.description.downcase.titleize
      - else
        %h1= @task.description.downcase.titleize
.row
  .col-md-12
    %ol.breadcrumb">
      %li= link_to 'Tasks',tasks_path
      %li.active= @task.description
